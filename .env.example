# Copie para .env e preencha: cp .env.example .env
# Não commitar .env (contém segredos).

# =============================================================================
# Azure OpenAI (recomendado para esta POC)
# =============================================================================
AZURE_OPENAI_API_KEY=
# URL base do recurso Azure OpenAI (sem path). Ex.: https://seu-recurso.openai.azure.com/
# Não usar /openai/responses - Agno usa Chat Completions.
AZURE_OPENAI_ENDPOINT=
# Nome do deployment - deve coincidir com o nome no Azure AI Studio (Deployments).
AZURE_OPENAI_DEPLOYMENT=gpt-4o
# api-version para Chat Completions. Use 2024-08-01-preview para evitar Responses API.
AZURE_OPENAI_API_VERSION=2024-08-01-preview
# Deployment de embedding (usado pelo Agno Knowledge para vetores no PgVector).
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small

# =============================================================================
# Multi-tenant (igual smart-squad-service)
# Organizações: TENANTS_CONFIG_JSON (JSON com chave "organizations") ou
# arquivo config/organizations.json / app/config/organizations.json.
# Formato: veja config.json.example (copie para organizations.json ou use em TENANTS_CONFIG_JSON)
# Em cada request, envie o header X-Tenant: <nome_do_tenant> para usar a config daquele tenant.
# Sem X-Tenant (ou tenant não configurado): modo simples, usa apenas variáveis de ambiente acima.
# =============================================================================
TENANTS_CONFIG_JSON=

# =============================================================================
# API keys fallback (quando não usar Azure OpenAI)
# =============================================================================
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# =============================================================================
# Agent UI: URL que o navegador usa para alcançar o AgentOS
# dev: localhost; prod: sua URL pública
# =============================================================================
NEXT_PUBLIC_AGENTOS_URL=http://localhost:8000

# =============================================================================
# Runtime: dev (reload) ou prd
# =============================================================================
RUNTIME_ENV=dev

# =============================================================================
# PostgreSQL (serviço agentos-db no Docker; use estes valores com compose)
# =============================================================================
DB_HOST=agentos-db
DB_PORT=5432
DB_USER=ai
DB_PASS=ai
DB_DATABASE=ai
